# Drata Risk Dashboard (Local)

A tiny Express app that proxies Drata's public risks endpoint and renders a table with expandable **customFields**.

## Prereqs
- Node.js 18+ recommended (works with Node 16+ using node-fetch@2)

## Setup
1. Clone this folder (or copy files).
2. Install deps:
   ```bash
   npm install